syntax = "proto3";

package api.common;

option go_package="github.com/synerex/synerex_alpha/api/simulation/common";

message Coord {
    double latitude = 1;
    double longitude = 2;
}

enum AgentType {
    PEDESTRIAN = 0;
    CAR = 1;
	TRAIN = 2;
	SIGNAL = 3;
}
