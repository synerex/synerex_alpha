# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*~

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Eclipse project settings
.project

# Binaries for server, providers, cli tool
cli/daemon/se-daemon
cli/se/se
monitor/monitor-server
nodeserv/nodeid-server
provider/ad/ad-provider
provider/fleet/fleet-provider
provider/multi/multi-provider
provider/taxi/taxi-provider
provider/user/user-provider
server/synerex-server

# Go module file
go.sum


#node lock
package-lock.json
