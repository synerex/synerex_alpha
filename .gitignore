# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Eclipse project settings
.project

# Binaries for server, providers, cli tool
cli/daemon/se-daemon
cli/se/se
monitor/monitor-server
nodeserv/nodeid-server
provider/ad/ad-provider
provider/fleet/fleet-provider
provider/multi/multi-provider
provider/taxi/taxi-provider
provider/user/user-provider
provider/datastore/datastore-provider
provider/gtfs-ptransit/ptransit-provider
provider/kota-bus/ecotan-provider
provider/map/map-provider
provider/marketing/marketing-provider
provider/onemile/onemile-provider
provider/routing/routing-provider
provider/simple-routing/srouting-provider
provider/rpa/meeting/meeting-provider
provider/rpa/user/rpa-user-provider
provider/rpa/selenium/selenium
server/synerex-server

# Go module file
go.sum